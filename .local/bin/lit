#!/bin/sh
# Change brightness in percentage

value="$(
    brightnessctl set -- "$1" |
    sed -n 's/.*(\([0-9]*\)%).*/\1/p'
)"

pkill -RTMIN+3 dwmblocks
notify-send -u low -h int:value:$value -t 3000 -r 1002 "${0##*/} $value%"
